import { TableSchema } from '../types/TableSchema';
import { Structure } from './Structure';
import { Table } from './Table';
export declare class Database {
    path?: string | undefined;
    static instance: Database;
    tables: Table<any, any>[];
    tablesPath: string;
    private constructor();
    static getInstance(path?: string): Database;
    createTable<StructureData>(name: string, structure: Structure<StructureData>): Table<StructureData, TableSchema<StructureData>>;
    getTableByName(tableName: string): Table<any, any> | undefined;
    deleteTableByName(tableName: string): boolean;
}
