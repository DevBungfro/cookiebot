import { Structure } from '../entities/Structure';
import ITableDataProps from '../interfaces/ITableDataProps';
import { TableSchema } from '../types/TableSchema';
export default function generateDataWithSave<StructureData, TableData extends TableSchema<StructureData>>(path: string, tableData: TableData, structure: Structure<StructureData>): TableData & ITableDataProps<TableData>;
