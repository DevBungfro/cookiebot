import { Structure } from "../entities/Structure";
import { TableSchema } from "../types/TableSchema";
export default function parseToRawData<StructureData>(data: TableSchema<StructureData>, structure: Structure<StructureData>): TableSchema<StructureData>;
