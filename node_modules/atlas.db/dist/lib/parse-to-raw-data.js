"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
function parseToRawData(data, structure) {
    const mapsAndsSets = Object.values(structure.props).map((sc) => sc.type).filter(c => [Map, Set].includes(c));
    if (!mapsAndsSets.length)
        return data;
    const structureKeys = Object.entries(structure.props)
        .filter(([ref, val], i) => [Map, Set].includes(val.type))
        .map(([ref, val]) => ref);
    mapsAndsSets.forEach((constructor, index) => {
        const value = Array.isArray(data[structureKeys[index]]) ? data[structureKeys[index]] : [];
        data[structureKeys[index]] = new constructor(value);
    });
    return data;
}
exports.default = parseToRawData;
