import { Structure } from "../entities/Structure";
import { TableSchema } from "../types/TableSchema";
export default function readFile<StructureData>(path: string, structure: Structure<StructureData>): TableSchema<StructureData>[];
