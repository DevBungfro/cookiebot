"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
function omitKeys(data, keys) {
    const object = Object.assign({}, data);
    keys.forEach((key, i) => {
        if (Object.keys(object).includes(key)) {
            delete object[key];
        }
    });
    return object;
}
exports.default = omitKeys;
