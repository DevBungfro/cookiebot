import { Structure } from "../entities/Structure";
import { TableSchema } from "../types/TableSchema";
export default function writeFile<StructureData, TableData extends TableSchema<StructureData>>(path: string, newRawData: TableData[], structure: Structure<StructureData>): void;
